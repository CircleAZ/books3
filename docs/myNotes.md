**Geo-coding boundries**

- Remove Pincode feild.
- remove Taluka and district layer.

**Customer location**

-Address needs to be based on map coordinates.
- All text address feild needs to be set automatically based on geo-coding boundaries.
- get village from Geo coding boundary in real time and update the feild.
- Get Taluka and district from openstreetmap based on map coordinates.


**Add Customer Form**

- Only map should remain to pick the geo-coordinates. Other feilds should be auto filled based on the geo-coordinates in realtime.using geocoding boundaries. Also , if user chnages the map coordinates, then all other feilds should be updated automatically.
- In edit mode, if geo-coordinates are chnaged, then all other feilds should be updated automatically.
